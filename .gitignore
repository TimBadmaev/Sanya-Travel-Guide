# macOS Finder metadata
.DS_Store

# Локальные настройки редактора и инструментов. Нужны только на машине
# разработчика и не должны попадать в публикуемый репозиторий (D-12
# REAL-DEVICE-QA-2026-09.md). Файлы, которые уже были добавлены в индекс
# раньше, .gitignore не убирает — их нужно один раз снять с учёта:
#   git rm -r --cached .idea .claude .mcp.json
.idea/
.claude/
.mcp.json
