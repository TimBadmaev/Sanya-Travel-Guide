# ITERATION-3-CLOSEOUT.md

> Закрывающий документ Итерации 3 «Главная „Сейчас“ + настройки поездки + расстояние».
> Версия: **2.0** · Дата: **2026-09-13**
> Источники: `CLAUDE.md`, `PRODUCT.md` v1.3, `MVP-UX-SPEC.md` (редакция 2026-09-13), `CONTENT-PLACES.md` v1.2, `ITERATION-3-IMPLEMENTATION.md` (§26 — фактический контракт, §26.8 — финализация), `ITERATION-3-LOCATION-REVIEW.md`, `ITERATION-2-CLOSEOUT.md` v2.0, фактический код `js/**`, `css/styles.css`, фактические `data/config.json` и `data/places.json`, журналы сеансов автоматической validation 2026-09-12 и прогон финальной validation 2026-09-13 (headless Chrome 152).
> **Версия 1.0 (2026-09-12)** фиксировала состояние до решений владельца и перечисляла три открытых решения. **Версия 2.0** фиксирует принятые решения, финализацию кода и документов, финальную validation и статус QA на реальном iPhone. История сеансов 1–2 сохранена (раздел 10.1–10.2).

---

## 0. Как читать

- **Итерация 3 реализовывалась в три захода.**
  1. План `ITERATION-3-IMPLEMENTATION.md` §§1–25 — «расстояние от центра района» (2026-09-12, до ~13:10).
  2. После QA на реальном iPhone — модель проживания по `ITERATION-3-LOCATION-REVIEW.md` (слои C0 + C1): `stay`, `spots`, `resolveOrigin()` (2026-09-12, 23:02–23:22).
  3. **Финализация по решениям владельца** — формат «менее 0,1 км» и синхронизация документов (2026-09-12 23:45 — 2026-09-13).
- Время — локальное время файлов на машине владельца (MSK, UTC+3).
- Проект не под `git`; «файл не менялся» подтверждается временем изменения файлов (раздел 10.4).
- **Реальный iPhone в этом документе не считается проверенным**, пока владелец не передаст результат (раздел 12).

---

## 1. Что реализовано

Объём по `PRODUCT.md` 11.2, строка 3: «Первый запуск, настройки поездки, состояния главной, расстояние от точки проживания (центр района — запасной вариант)». Критерий: «Смена дат меняет главную; сортировка по расстоянию корректна».

| # | Пункт | Где в коде | Заход |
|---|---|---|---|
| 1 | Хранение поездки `stg:trip` — `getTrip()` / `setTrip()` с защитой от повреждённых значений | `js/storage.js` | 1, 2 (`stay`) |
| 2 | Маршрут `#/settings` (подсвечивает вкладку «Сейчас») | `js/app.js` | 1 |
| 3 | Экран настроек = экран первого запуска (одна форма, два режима) | `js/views/settings.js` | 1 |
| 4 | Выбор района из `config.areas` (только `verified`, включая `other`) | `js/views/settings.js` | 1 |
| 5 | Необязательный подвопрос «Где именно?» — ориентиры района | `js/views/settings.js`, `data/config.json` → `areas[].spots` | 2 |
| 6 | `resolveOrigin(trip, config)` — единственное место с правилом «откуда считать» | `js/logic/trip.js` | 2 |
| 7 | `haversineKm()` и `formatDistance()` — чистый модуль; **«менее 0,1 км»** | `js/logic/distance.js` | 1, **3** |
| 8 | `sortByDistance()` — стабильная, без мутации входа | `js/logic/filters.js` | 1 |
| 9 | «Места»: расстояние в строке, сортировка от ближнего, плашка над результатами (3 варианта) | `js/views/places.js` | 1, 2 |
| 10 | Карточка: «~N км от места проживания» / «~N км от центра района» | `js/views/place.js` | 1, 2 |
| 11 | «Сейчас»: 4 состояния по датам, сценарии, прогресс, ближайшие дела, «Сохранено: N», «Под рукой» | `js/views/home.js`, `js/logic/trip.js` | 1 |
| 12 | Первый запуск — редирект `location.replace` с `#/`, не чаще раза за сессию страницы | `js/views/home.js` | 1 |
| 13 | `ctx.from` в роутере — у «Настроек» больше одного родителя | `js/app.js` | 2 |
| 14 | Строка пункта чек-листа вынесена для повторного использования на Главной | `js/views/task.js`, `js/views/prepare.js` (только импорт) | 1 |
| 15 | Стили — только новые правила в конце файла (блок «Итерация 3») | `css/styles.css` | 1, 2 |
| 16 | Продуктовые и контентные документы синхронизированы с утверждённой моделью | `PRODUCT.md`, `MVP-UX-SPEC.md`, `CONTENT-PLACES.md`, `ITERATION-3-IMPLEMENTATION.md` | **3** |

**Не менялись Итерацией 3:** `index.html`, `js/data.js`, `js/views/info.js`, `js/logic/checklist.js`, `data/places.json`, `data/checklist.json`, `data/info.json`.
**Контент:** `data/config.json` — добавлено необязательное поле `spots` у 4 районов (второй заход); в третьем заходе JSON не менялся.
**Сознательно не реализовано (вне объёма):** GPS и «Рядом со мной», UI для `stay.source = "gps" | "manual"`, карта, поиск, PWA/Service Worker, расширение базы до 25–40 мест, расширение `spots` до 15–25 точек, кнопка Amap, быстрый переключатель района на Главной (кандидат в Q-14), раскрытие этапов «Подготовки» по датам.

---

## 2. Что исправлено

### 2.1 Первый заход (до ревью модели проживания)

| # | Проблема | Как проявлялась | Исправление |
|---|---|---|---|
| 1 | После редиректа первого запуска «Готово» уводил из приложения | `ctx.back("#/")` делал `history.back()` за пределы приложения | Редирект на `#/settings?first=1`; при этом маркере выход через `location.replace("#/")` |
| 2 | Двойное округление в `formatDistance` | 14,479 км давало «~15 км» | Округление до 0,1 только выбирает формат; целое — от исходных км ([К3-6]) |

### 2.2 Второй заход — после QA на реальном iPhone

**Исходный дефект QA:** отель в Ялунване, пляж Ялунвань — «~3,5 км». **Причина — модель:** район был одновременно логическим контекстом и точкой отсчёта (центр `yalong` — точка Wikidata, пляж — точка OSM; между ними 3,534 км).

| # | Проблема | Исправление |
|---|---|---|
| 3 | Точка отсчёта = центр района; место проживания выразить нельзя | `stg:trip.stay` + `resolveOrigin()` + справочник `config.areas[].spots` |
| 4 | Подпись «~3,5 км от вашего района» обещала больше, чем есть | Подпись по точности: «от места проживания» / «от центра района» |
| 5 | В списке не было видно, что расстояние условное | Плашка при `precision: "area"` со ссылкой «Уточнить, где вы живёте» |
| 6 | «Рядом с отелем» проверял район, а не точку | Условие `precision === "none"` → `#/settings`, иначе `#/places` |
| 7 | «Готово» в настройках, открытых со «Мест», терял фильтры и прокрутку | `ctx.from` в роутере; выход `ctx.back(ctx.from \|\| "#/")` |
| 8 | Текст первого запуска «…расстояние от вашего района» | «…расстояние от места вашего проживания» |

### 2.3 Третий заход — финализация (решение владельца №2)

| # | Проблема | Исправление |
|---|---|---|
| 9 | При выборе любого ориентира ближайшее место показывалось как «~0,0 км» (все 6 ориентиров совпадают с координатами мест); у Дадунхая без уточнения — тоже | `formatDistance()`: если округление до 0,1 даёт 0 — «менее 0,1 км». Расчёт, сортировка, `resolveOrigin()`, `stay`, экраны не менялись |

**Дефектов кода в финальной validation не найдено** (раздел 10.3) — других исправлений кода в третьем заходе не было.

---

## 3. Принятые решения владельца (2026-09-13)

| № | Решение | Что это закрывает | Где зафиксировано |
|---|---|---|---|
| 1 | **Текущий набор из 6 `spots` сохраняется как MVP.** Расширение до 15–25 точек в этой задаче не выполняется | Решение 1 версии 1.0; [L-1] для MVP; `CONTENT-PLACES.md` §8 №8 | `PRODUCT.md` 9.4, журнал 1.3; `CONTENT-PLACES.md` §2.1; `ITERATION-3-IMPLEMENTATION.md` §26.8 |
| 2 | **Вместо «~0,0 км» — «менее 0,1 км».** Числовые расчёты и сортировка не меняются; остальные расстояния — в прежнем формате | Решение 2 версии 1.0; [L-6], [OQ3-3], [OQ-17] (для текущего порога) | `PRODUCT.md` D-11, 8.4; `MVP-UX-SPEC.md` §4, §5, §12, §14; `ITERATION-3-IMPLEMENTATION.md` §26.8 |
| 3 | **Утверждена модель:** `trip.area` — логический контекст района; `trip.stay` — точка проживания / точка отсчёта; `resolveOrigin()` определяет фактический origin; при отсутствии конкретной точки допускается запасной вариант `area.center` с пометкой «от центра района» | Решение 3 версии 1.0; конфликты C-1…C-12 версии 1.0 | `PRODUCT.md` D-11 (изменено), 7, 8.4, 8.5, 9.4, 9.7, 11, 16, журнал 1.3; `MVP-UX-SPEC.md` §1.1, §4, §5, §8, §10, §12 |

**Не решалось владельцем в этой задаче (остаётся открытым):** `yalong.center` по Wikidata ([L-2]); точка центра `city`; район и цена Тяньяхайцзяо; адрес и `verified` Первого рынка; цена Лухуйтоу; Q-02; кандидат в Q-14. Влияние утверждённой модели на эти вопросы описано в `CONTENT-PLACES.md` §2, §5.5, §5.9, §7, §8.1.

---

## 4. Модель проживания (утверждённая)

### 4.1 Роли

| Сущность | Роль | Где хранится | Где используется |
|---|---|---|---|
| **Район** (`trip.area`) | Логический контекст: «я живу в Ялунване» | `stg:trip.area` — только `id` | Подписи на Главной, выбор ориентиров в настройках, запасная точка отсчёта (`center`) |
| **Точка проживания** (`trip.stay`) | Точка отсчёта расстояний | `stg:trip.stay` — необязательный объект с явным `source` | Только через `resolveOrigin()` |

### 4.2 Схема `stg:trip`

```json
{ "start": "2026-11-12", "end": "2026-11-27", "area": "yalong",
  "stay": { "source": "spot", "spotId": "yalong-beach-front" } }
```

| Поле | «Не задано» | Правило |
|---|---|---|
| `start`, `end` | `null` | `ГГГГ-ММ-ДД`, реально существующая дата (иначе молча `null`); `start > end` → состояние «Без дат» |
| `area` | `null` | Непустая строка; существование проверяет `resolveOrigin()` / view |
| `stay` | `null` | `source: "spot"` → только `spotId` (D-20). `source: "manual" \| "gps"` → `point {lat,lng}` в диапазонах, у `gps` необязательный `updatedAt`. UI Итерации 3 создаёт **только** `spot` |

- Ключ существует (даже со всеми `null`) = первый запуск пройден. `stg:schema` = `1`, миграций нет: значение без `stay` читается как `stay: null`.
- Повреждённый `stay` (не объект, неизвестный `source`, `spot` без `spotId`, координаты-строки или вне диапазона) → «точки нет» + один `console.error`; **сырое значение не перезаписывается**.
- `setTrip()` собирает объект по белому списку `start/end/area/stay`; `settings.js` передаёт `stay` целиком — сохранение только дат `stay` не стирает (проверено, раздел 10.3).

### 4.3 Ориентиры `config.areas[].spots` (утверждены как MVP)

| Район | `id` → подпись | Координата | Совпадает с |
|---|---|---|---|
| `dadonghai` | `dadonghai-beach-front` → «У пляжа Дадунхай» | 18.218626, 109.517611 | `dadonghai-beach.location` **и** `dadonghai.center` |
| `dadonghai` | `dadonghai-luhuitou` → «Со стороны парка Лухуйтоу» | 18.227154, 109.498405 | `luhuitou-park.location` |
| `yalong` | `yalong-beach-front` → «У пляжа Ялонг Бэй» | 18.224358, 109.624165 | `yalong-bay-beach.location` |
| `yalong` | `yalong-forest-park` → «У лесного парка «Тропический рай»» | 18.253056, 109.639624 | `yalong-tropical-forest-park.location` |
| `sanya-bay` | `sanya-bay-beach-front` → «У пляжа Санья Бэй (Кокосовая аллея)» | 18.261233, 109.490052 | `sanya-bay-beach.location` |
| `city` | `city-first-market` → «У Первого рынка (исторический центр)» | 18.240184, 109.504561 | `sanya-first-market.location` |
| `haitang`, `other` | — | — | Ориентиров нет, подвопрос не показывается |

Новых координат не введено: у каждого ориентира `source` места и `verifiedAt: 2026-09-11`. Совпадение с координатами проверено расчётом по фактическим JSON 2026-09-13. Источники и следствия — `CONTENT-PLACES.md` §2.1.

### 4.4 Поведение формы

- «Где именно?» — вложенная radio-группа под выбранным районом с `spots`; блоков в форме два (MVP-UX-SPEC §8).
- «Не знаю — считать от центра района» — последний вариант и значение по умолчанию (`stay: null`).
- Смена района сбрасывает точку; возврат к исходному району до сохранения возвращает сохранённую точку; после сохранения с другим районом — `stay: null`.
- `gps`/`manual` в `stay` (UI их не создаёт) форма показывает как «Не знаю», значение при сохранении не теряется.

---

## 5. `resolveOrigin()`

`js/logic/trip.js`, чистая функция (без DOM, `storage`, `location`). Вход: `trip` из `storage.getTrip()`, `config` из `loadConfig()` (только `verified`-районы). Выход: `{ point, precision, label }`.

| Шаг | Условие | `point` | `precision` | `label` |
|---|---|---|---|---|
| 1–2 | `stay.source` = `"gps"` / `"manual"` | `stay.point` | `exact` | `null` |
| 3 | `stay.source` = `"spot"`, район найден, `spotId` есть в **его** `spots` | `spot.point` | `exact` | `spot.name` |
| 4 | Шаги 1–3 не дали точку, район найден, `center` не `null` — **запасной вариант** | `area.center` | `area` | `area.name` |
| 5 | Район не выбран, не найден или `center: null` (`other`) | `null` | `none` | `null` |

Подтверждено финальной validation (раздел 10.3, группа 1): исчезнувший `spotId` → `area`; ориентир чужого района → `area` от центра выбранного; ориентир у района без `spots` (`haitang`) → `area`; `gps` при `other` → `exact`; `manual` без района → `exact`; `trip = null` и отсутствие `config` → `none`.

**Потребители:** `places.js` (точка + плашка), `place.js` (точка + подпись), `home.js` (только `precision` для «Рядом с отелем»). Больше нигде `area.center` для расчёта не читается.

---

## 6. Расстояние: расчёт и отображение

### 6.1 Расчёт (не менялся в третьем заходе)

- `haversineKm(origin, place.location)` — по прямой, `R = 6371` км, WGS-84; неверная точка → `NaN`.
- Считается до `place.location`; район места не участвует — Наньшань (`area: "other"`) получает расстояние, если у пользователя есть точка отсчёта.
- Список: `applyFilters()` → `sortByDistance()`; расстояние — один раз на место за рендер; ничего не кэшируется между рендерами.
- `sortByDistance(places, null)` → копия в порядке `places.json`; `NaN` и равные расстояния → стабильно, порядок файла.
- **Сортировка — по исходным километрам**, не по тексту: несколько мест в пределах «менее 0,1 км» упорядочиваются по реальному расстоянию (проверено, раздел 10.3).
- Время в пути не показывается нигде (D-11).

### 6.2 Формат `formatDistance(km)` — после финализации

| Правило | Примеры (проверено) |
|---|---|
| не конечное число → `""` (блок не выводится) | `NaN`, `Infinity`, `undefined`, `null` → `""` |
| `Math.round(km × 10) / 10 === 0` → **«менее 0,1 км»** | 0 → «менее 0,1 км»; 0,001; 0,04; 0,0499 → «менее 0,1 км» |
| `< 10` → один знак, запятая | 0,05 → «~0,1 км»; 0,0999 / 0,1 / 0,149 → «~0,1 км»; 0,449 → «~0,4 км»; 3,534 → «~3,5 км»; 9,94 / 9,949 → «~9,9 км» |
| иначе → `Math.round(km)` от исходного значения | 9,95 / 9,96 → «~10 км»; 10,151 → «~10 км»; 14,479 → «~14 км»; 48,318 → «~48 км» |

### 6.3 Отображение по точности

| `precision` | Строка `#/places` | Плашка над результатами | Карточка `#/place/<id>` | «Рядом с отелем» |
|---|---|---|---|---|
| `exact` | «менее 0,1 км · …» / «~N км · …», сортировка от точки | нет | «менее 0,1 км от места проживания» / «~N км от места проживания» | → `#/places` |
| `area` | То же, от центра района | «Расстояния — от центра района, это приблизительно. Уточнить, где вы живёте» → `#/settings` | «~N км от центра района» (у Дадунхая — «менее 0,1 км от центра района») | → `#/places` |
| `none`, район не выбран | без расстояния, порядок файла | «Выберите район, чтобы видеть расстояние» → `#/settings` | блока нет | → `#/settings` |
| `none`, район `other` | без расстояния, порядок файла | «Для района «Другое / за пределами центральных районов» расстояние не считается — у него нет центра на карте.» | блока нет | → `#/settings` |

Экран «Показать таксисту» расстояние не показывает (проверено).

### 6.4 Контрольные значения на фактических данных (совпали в интерфейсе 2026-09-13)

| Место | От `yalong.center` (`area`) | От `yalong-beach-front` (`exact`) |
|---|---|---|
| `yalong-bay-beach` | 3,534 → ~3,5 км | 0,000 → **менее 0,1 км** |
| `yalong-tropical-forest-park` | 4,949 → ~4,9 км | 3,584 → ~3,6 км |
| `wuzhizhou-island` | 10,151 → ~10 км | 11,835 → ~12 км |
| `dadonghai-beach` | 14,479 → ~14 км | 11,272 → ~11 км |
| `sanya-first-market` | 16,163 → ~16 км | 12,754 → ~13 км |
| `sanya-song-cheng` | 16,247 → ~16 км | 12,763 → ~13 км |
| `luhuitou-park` | 16,581 → ~17 км | 13,286 → ~13 км |
| `sanya-bay-beach` | 18,244 → ~18 км | 14,745 → ~15 км |
| `tianya-haijiao` | 33,651 → ~34 км | 30,174 → ~30 км |
| `nanshan-cultural-zone` | 48,318 → ~48 км | 44,886 → ~45 км |

Порядок от ориентира: пляж → лесопарк → **Дадунхай → Улучжичжоу** → …; от центра залива Улучжичжоу выше Дадунхая. От `dadonghai.center`: «менее 0,1 км» → ~2,2 → ~2,8 → ~5,6 → ~8,2 → ~11 → ~13 → ~20 → ~23 → ~34 км. От `dadonghai-luhuitou`: «менее 0,1 км» (Лухуйтоу) → ~1,6 км → ~2,2 км. Километры до изменения формата и после совпадают.

---

## 7. `#/settings`

| Свойство | Фактическое поведение |
|---|---|
| Маршрут | `/^\/settings$/`, подсвечена «Сейчас», не fullscreen |
| Данные | `loadConfig()` через Promise-кэш; ошибка → «Не удалось загрузить данные» + «Повторить» |
| Первый запуск (`?first=1`) | «Когда и где вы будете?»; «Готово» + «Пропустить всё» |
| Обычный режим | «Поездка»; только «Готово»; поля предзаполнены из `stg:trip` |
| Блок 1 «Когда поездка?» | Два `<input type="date">`, 16 px |
| Блок 2 «Где вы живёте?» | «Пока не выбрано» + районы; вложенный «Где именно?» (раздел 4.4) |
| «Готово» | `setTrip({ start, end, area, stay })`; при неудаче — «Не удалось сохранить: хранилище браузера недоступно.» |
| «Пропустить всё» | `setTrip` со всеми `null` — первый запуск больше не показывается |
| Выход | `?first=1` → `location.replace("#/")`; иначе `ctx.back(ctx.from \|\| "#/")` — туда, откуда пришли, с фильтрами и прокруткой; глубокая ссылка → `replace("#/")`; история не растёт |
| Входы | ⚙️ на Главной (≥44×44); «Указать даты» / «Обновить даты»; «Рядом с отелем» при `none`; плашки на «Местах» |

---

## 8. «Сейчас»

Состояние — **только по датам** (`getTripState(trip, getTodayIso())`, локальная дата устройства).

| Состояние | Условие | Что показывается |
|---|---|---|
| **Без дат** | Нет `start`/`end` или `start > end` | «Укажите даты…» + «Указать даты»; прогресс, если что-то отмечено; «Хочу на пляж», «Хочу природу», «Куда сходить с детьми» |
| **До поездки** | сегодня < `start` | «До поездки: N дней»; «Район: …»; прогресс; до 3 невыполненных дел с отметкой на месте; «Весь чек-лист» |
| **В поездке** | `start` ≤ сегодня ≤ `end` | «День N из M · <район>»; «Рядом с отелем» (по `precision`), «Есть 2–3 часа», «Хочу природу», «В помещении»; «Сохранено: N мест»; «Такси и Amap», «Экстренные номера» |
| **После поездки** | сегодня > `end` | Компонент «Без дат» + «Поездка завершена — даты можно обновить для следующей поездки.» + «Обновить даты» |

---

## 9. Синхронизированные документы (третий заход)

| Документ | Версия | Что изменено |
|---|---|---|
| `PRODUCT.md` | 1.1/1.2 → **1.3** | D-11 (изменено решением владельца); 7 Must have №4; 8.4 (строка, «Рядом», таблица точек отсчёта, формат с «менее 0,1 км»); 8.5 п. 3; 9.4 (`spots`); 9.7 (`stay`); 11.1 (текущий статус; история Итерации 1 сохранена); 11.2 (статусы Итераций 2 и 3, описание 3 и 5); R-08; словарь; журнал 1.3; заголовок приведён к текущей версии |
| `MVP-UX-SPEC.md` | редакция 2026-09-13 | §1.1 (D-11), §1.2 п. 6, §4 (таблица `precision`, 3 плашки), §5 п. 3, §8 («Где именно?»), §10 (`spots`, `stay`), §12 (новые edge cases), §14, §15.B, §15.E (кандидат в Q-14 — не решён), статус. Раздел «Проверка согласованности» помечен историческим |
| `CONTENT-PLACES.md` | 1.1 → **1.2** | §2.1 — 6 `spots` с источниками и датами; решения/состояние по `city` (§2 прим. 1), Ялунваню (прим. 3), `other` (прим. 2), Тяньяхайцзяо (§5.5), Первому рынку (§5.9); связи координат мест с ориентирами; §7 риски под новую модель; §8 №8; §8.1 на 2026-09-13 с ценами; §12 журнал |
| `ITERATION-3-IMPLEMENTATION.md` | — | Заголовок: §§1–25 — история, §26 — контракт; пометки «заменено §26» у формулировок «от центра района»; [OQ3-3] закрыт; §26.6 и §26.7 обновлены; **§26.8 «Финализация»**; статус |
| `ITERATION-3-CLOSEOUT.md` | 1.0 → **2.0** | Этот документ |

**Сверка `CONTENT-PLACES.md` с JSON (2026-09-13):** 6 `spots`, их координаты, `source` и `verifiedAt`; координаты всех 10 мест и 5 центров; цены (158/140 CNY лесопарк, 129/108 CNY Наньшань, Улучжичжоу и «Тысячелетняя любовь» — без цифр, остальные `free`); статусы (`verified` у всех 10); `area` Тяньяхайцзяо (`sanya-bay`); адрес Первого рынка (新建街155号). Расхождений не найдено, новых фактов не добавлено.

**Не менялись (исторические или вне задачи):** `CLAUDE.md`, `README.md`, `ITERATION-1-CLOSEOUT.md`, `ITERATION-2-FOUNDATION.md`, `ITERATION-2-CLOSEOUT.md`, `ITERATION-3-LOCATION-REVIEW.md`, `PLACES-IMPLEMENTATION.md`, `INFO-IMPLEMENTATION.md`, `CONTENT-INFO.md`.

---

## 10. Автоматическая validation

Окружение всех сеансов: headless Google Chrome 152 через CDP-клиент на Python stdlib, мобильная эмуляция 320×568 и 375×667, проект отдаётся локальным многопоточным HTTP-сервером на 127.0.0.1. Реальные устройства не использовались.

### 10.1 Сеанс 1 — исходная реализация (2026-09-12, ~12:50–13:10) — история

370 проверок в 9 наборах (логика 36, хранилище 18, места 30, settings/история 45, главная 46, регрессия 35, мобильный 136, потоки 19 + 5), все зелёные в финальном прогоне. Найдены и исправлены дефекты 1–2 (раздел 2.1). Сеанс проверял модель «район = точка», поэтому дефект «~3,5 км» не ловил. Подробности — версия 1.0 этого документа, §8.1.

### 10.2 Сеанс 2 — модель проживания C0 + C1 (2026-09-12, 23:02–23:22) — история

Сценарные прогоны `t1`–`t12` без счётчиков (~120 проверок по оценке сеанса). Найден и исправлен дефект 7 (`ctx.from`). Ложные срабатывания: overflow на прокручиваемом ряду чипов, `ERR_CONNECTION_RESET` однопоточного сервера, размер самого чекбокса вместо зоны нажатия. Подробности — версия 1.0, §8.2.

### 10.3 Сеанс 3 — финальная validation после «менее 0,1 км» (2026-09-13)

Один сценарий `val_all.py` с явными PASS/FAIL. Прогон на коде после единственного изменения (`js/logic/distance.js`, 2026-09-12 23:45).

**Итог финального прогона: 278 PASS / 0 FAIL, 0 сетевых сбросов.**

| Группа | Итог | Что проверено |
|---|---|---|
| 0. Статические инварианты | 6 / 0 | Нет `location.hash =` и `pushState` в `js/`; `fetch(` только в `js/data.js`; `js/logic` без `document`/`localStorage`/`window.`; внешних URL нет (кроме комментария в `place.js`); «~0,0» в коде только в комментарии |
| 1. Логика | 48 / 0 | `resolveOrigin` × 12 входов; порядок и формат от `yalong-beach-front`, `yalong.center`, `dadonghai.center`; км не изменились; без точки — порядок файла; вход не мутируется; сортировка внутри «менее 0,1 км» по сырым км; стабильность; `NaN`; `formatDistance` × 22 значения (границы 0,0499/0,05, 9,949/9,95, `NaN`/`Infinity`/`undefined`/`null`) |
| 2. Сценарий Ялунваня через UI | 19 / 0 | Первый запуск → Ялунвань → «Где именно?» (2 ориентира + «Не знаю» по умолчанию) → «У пляжа Ялонг Бэй» → Готово → `stg:trip` целиком, `stg:schema = 1` → Места: первым «Пляж Ялонг Бэй», «менее 0,1 км · …», нет «~3,5 км» и «~0,0», вся колонка расстояний и порядок как в §6.4, плашки нет → карточка «менее 0,1 км от места проживания» между параметрами и описанием → Back → лесопарк «~3,6 км» → reload — те же числа |
| 3. Точности | 15 / 0 | Ялунвань без точки: §8.1, плашка со ссылкой, «~3,5 км от центра района»; Дадунхай без точки: «менее 0,1 км» и «менее 0,1 км от центра района»; от Лухуйтоу; `other`: без км, плашка без ссылки, без блока на карточке; без поездки; `haitang` — `area`, без «Где именно?» |
| 4. Settings и `stg:trip` | 14 / 0 | Обычный режим; предзаполнение дат, района и точки; 16 px в полях дат; сохранение дат не стирает `stay`; смена района и возврат до сохранения; сохранение с другим районом → `stay: null`; «Пропустить всё»; первый запуск не повторяется; 4 повреждённых значения — экран жив, ошибок в консоли по правилу, сырое значение цело |
| 5. Back/Forward | 15 / 0 | Места(`f=beach,nature`) → прокрутка → плашка → ориентир → Готово → тот же список, фильтры, пересчитанный порядок, плашка исчезла, прокрутка восстановлена (с поправкой на укоротившуюся страницу), история не выросла; Forward → настройки с ориентиром; Back → список; карточка → Back → фильтры и прокрутка; чип без роста истории; глубокая ссылка на `#/settings`; «← К местам» |
| 6. «Сейчас» | 14 / 0 | 4 состояния по датам относительно реального «сегодня»; ⚙️ 44×44; «Рядом с отелем» по `precision` (`area` → места, `other` → настройки, `exact` → места с «менее 0,1 км» первым) и Back; «Сохранено: 2 места»; отметка дела обновляет прогресс и `stg:checklist` |
| 7. Prepare, Info, таксист, копирование | 11 / 0 | «Подготовка»: отметка на месте и после перезагрузки, зона чекбокса 44×44; «Справка»: 5 карточек, деталь с источниками; таксист: fullscreen (шапка и панель скрыты), расстояния нет, копирование в буфер (`亚龙湾` + адрес) и «Скопировано ✓» с возвратом через ~2 с; «Закрыть» → карточка, панель вернулась, Back не возвращает на таксиста |
| 8. 320×568 и 375×667 | 134 / 0 | 10 маршрутов × 3 состояния поездки (`exact`, Дадунхай `area`, `other`) × 2 ширины: отрисовка, нет горизонтального overflow, тач-цели ≥44 (radio/checkbox — по зоне нажатия), шрифт расстояния на карточке ≥16 px; «менее 0,1 км от места проживания» и «менее 0,1 км · …» помещаются без обрезки на обеих ширинах; safe area (эмуляция insets 47/34) — sticky на месте; консоль без ошибок приложения |
| 9. Данные | 2 / 0 | Каждый JSON запрошен не больше одного раза за сессию при переходах по 7 экранам; все ресурсы приложения — 200 |

**Промежуточные падения этого сеанса и их природа** (код между прогонами не менялся — единственное изменение кода 23:45, прогоны 2026-09-13 после него):

| Прогон | Что упало | Природа | Как установлено |
|---|---|---|---|
| 1 (263 PASS / 32 FAIL) | «~0,0» в `distance.js` | Тест: совпадение в новом комментарии | Строка — комментарий; проверка уточнена |
| 1 | Место без координат «в конце» | Тест: неверное ожидание; фактическое поведение — порядок файла при `NaN`, так и задокументировано (§6.1) | Контракт `sortByDistance` |
| 1 | Невалидная дата не даёт `console.error` | Тест: `storage.js` молча читает неверную дату как `null` (ошибки пишутся только для `stay`) | Код `isValidDateString` |
| 1 | Прокрутка 187 вместо 260 | Тест: после исчезновения плашки страница короче, 187 — новый максимум; сравнение переделано на `min(сохранённая, максимум)` | Замер `scrollHeight − innerHeight` |
| 1 | «Статус копирования пуст» | Тест: успех показывается текстом кнопки «Скопировано ✓», `role=status` — только для неудачи | Код `bindCopyButton` |
| 1 | Шрифт 14 px в строке списка и «Справке» | Тест мерил подпись строки `.info-card__summary` (`--font-size-sm`, стиль Итераций 1–2, общий для «Мест» и «Справки»); проверка ограничена карточкой (16 px). **Не изменение этой задачи** — см. раздел 14 | `css/styles.css:499` |
| 1 | Чекбокс «Подготовки» 24×24 | Тест мерил сам чекбокс, а не обёртку `task__checkbox-wrap` 44×44 (группа 7 — 44×44) | Как в сеансе 2 |
| 1, 3 | `ERR_CONNECTION_RESET`, один маршрут не отрисовался | Окружение: сброс соединений локального сервера при холодных загрузках | Повторные попытки проходят; все ресурсы 200 |
| 2 | Нет редиректа первого запуска → трассировка | Тест: фиксированная пауза 400 мс короче асинхронного рендера; заменено ожиданием | В прогонах 1 и 3 редирект есть |
| 3 (277 / 1) | Лишняя ошибка при повреждённом `gps` | Окружение: к ожидаемой ошибке `stay` добавился `ERR_CONNECTION_RESET` | Ожидаемая ошибка присутствует; сетевые сбросы исключены из подсчёта |
| 4 (277 / 1) | Консоль mobile-прогона: `ERR_SOCKET_NOT_CONNECTED`, 26 `ERR_CONNECTION_RESET` | Окружение: тестовый сервер с очередью соединений по умолчанию (5) не выдерживал холодных загрузок ES-модулей; кроме того, порт держал старый экземпляр сервера | Сервер перезапущен с `request_queue_size = 128`; финальный прогон 5 — **278 / 0, сбросов 0**. Код приложения не менялся |

**Реальных дефектов кода финальная validation не выявила.**

### 10.4 Изменённые файлы третьего захода

По времени изменения (всё, что новее версии 1.0 этого документа, 2026-09-12 23:37, без `.idea/`): `js/logic/distance.js` (23:45), `PRODUCT.md`, `MVP-UX-SPEC.md`, `ITERATION-3-IMPLEMENTATION.md`, `CONTENT-PLACES.md` (2026-09-13 00:12–00:16), `ITERATION-3-CLOSEOUT.md`. Других файлов проекта третий заход не менял; тестовые скрипты лежат вне проекта.

---

## 11. Regression

| Область | Результат | Где подтверждено |
|---|---|---|
| Foundation: маршруты, `parseHash`, `{key, from}`, прокрутка по записи истории, `ctx.back()` | Не сломано | Сеанс 3, группы 5, 7, 8 |
| Инварианты истории: нет `location.hash =`, нет `pushState` | Соблюдены | Сеанс 3, группа 0 |
| Promise-кэш `data.js`, `fetch` только там | Не сломано | Сеанс 3, группы 0, 9 |
| «Места»: чипы, `f=` в URL, «Найдено N», `replaceState` без роста истории | Не сломано | Сеанс 3, группы 2, 3, 5 |
| Карточка: порядок блоков, sticky, избранное | Не сломано | Сеанс 3, группы 2, 6, 8 |
| «Показать таксисту», fullscreen, копирование | Не сломано | Сеанс 3, группа 7 |
| «Подготовка» | Работает, отметки сохраняются | Сеанс 3, группы 6, 7, 8 |
| «Справка» | Работает | Сеанс 3, группы 7, 8 |
| `localStorage`: `stg:schema = "1"`, `stg:trip`, `stg:checklist`, `stg:saved` | Не сломано | Сеанс 3, группы 2, 4, 6, 7 |
| Нижняя навигация | Не сломана | Сеансы 2, 3 |
| Горизонтальный overflow 320 / 375 px | Нет | Сеанс 3, группа 8 |

Изменение формата затрагивает только текст расстояния в строке списка и на карточке; все прочие контрольные строки формата совпали с сеансами 1–2.

---

## 12. QA на реальном iPhone

### 12.1 Что было

- **Итерация 2:** QA на реальном iPhone выполнен владельцем без существенных замечаний (`ITERATION-2-CLOSEOUT.md` §11).
- **Итерация 3, первый заход:** QA на реальном iPhone выявил дефект «~3,5 км» в Ялунване → второй заход (раздел 2.2).

### 12.2 Повторный QA после финализации — **ожидает результата владельца**

**Статус: не выполнен / результат не получен.** Этот документ не утверждает, что iPhone проверен.

Сценарий, который нужно пройти на реальном iPhone:

`Ялунвань → Где именно? → У пляжа Ялонг Бэй → Места → Ялонг Бэй`

| # | Что проверить | Ожидаемый результат (по эмуляции) | Результат на iPhone |
|---|---|---|---|
| 1 | Нигде нет «~3,5 км» | В списке у пляжа — не «~3,5 км» | *ожидает* |
| 2 | У пляжа Ялонг Бэй — «менее 0,1 км» | Строка «менее 0,1 км · …»; карточка «менее 0,1 км от места проживания» | *ожидает* |
| 3 | Порядок мест соответствует новой точке | Пляж → лесопарк (~3,6 км) → Дадунхай (~11 км) → Улучжичжоу (~12 км) → … | *ожидает* |
| 4 | «Назад» возвращает корректно | С карточки — в список; из настроек, открытых по плашке, — в тот же список | *ожидает* |
| 5 | Фильтры и прокрутка сохраняются | После карточки и после «Готово» — те же чипы и позиция | *ожидает* |
| 6 | Поля дат | Нативный выбор даты, очистка, без зума при фокусе | *ожидает* |
| 7 | Свайп-назад | Ведёт себя как «Назад»; после первого запуска не зацикливается | *ожидает* |
| 8 | Fullscreen «Показать таксисту» | Шапка и панель скрыты; «Закрыть» возвращает их | *ожидает* |
| 9 | Копирование | «Скопировать» кладёт текст в буфер, видна реакция | *ожидает* |
| 10 | Safe area | Нет перекрытия вырезом и home-индикатором, sticky-кнопки доступны | *ожидает* |

Желательно указать в результате модель iPhone, версию iOS и браузер (Safari или другой).

---

## 13. Что осталось непроверенным

1. **Реальный iPhone после финализации** — весь раздел 12.2.
2. **Safari iOS** как движок: нативный `<input type="date">`, свайп-назад, вложенные radio на тач-экране, реальная safe area, отсутствие зума.
3. **Chrome Android и Яндекс Браузер (D-31)** — не проверялись ни в одной итерации.
4. **Реальный приватный режим** — недоступный `localStorage` моделировался подменой (сеанс 2), в сеансе 3 не перепроверялся.
5. **Смена «сегодня»** при открытой вкладке (полночь) и часовой пояс Москва ↔ Санья.
6. **Запасной путь копирования** по `http://192.168.x.x` — с Итерации 2 не перепроверялся.
7. **Публикация** на GitHub Pages и доступность из Китая (Q-04).
8. **Холодный старт без сети** — только экран ошибки (Service Worker — Итерация 4).
9. **Screen reader** для вложенных `fieldset`/`legend`, плашек и `role="status"`.
10. **Ветки `gps` / `manual`** — только через хранилище и `resolveOrigin()`; UI — Итерация 5.
11. **Попиксельное сравнение** «Подготовки» и «Справки» с baseline — нет снимка и `git`.

---

## 14. Ограничения и известные расхождения, которые остаются

**Ограничения модели и данных:**

1. **Все 6 ориентиров — координаты мест**, а не отелей. Тот, кто живёт между ориентирами, выбирает ближайший или «Не знаю».
2. **`dadonghai-beach-front` = центр `dadonghai`:** выбор ориентира не меняет чисел, только подпись и плашку.
3. **У `haitang` нет ориентиров** — только запасной режим (до причала Улучжичжоу 4,045 км от центра).
4. **Запасной режим остаётся условным:** «~3,5 км от центра района» до пляжа в Ялунване без уточнения — это ожидаемо и подписано; `yalong.center` по Wikidata не менялся ([L-2] открыт).
5. **Один район и одна точка на всю поездку**; быстрого переключения на Главной нет (кандидат в Q-14 открыт).
6. **База — 10 мест** (D-05: 25–40); после смены точки прокрутка восстанавливается в тех же пикселях, но порядок уже другой ([OQ3-8]).
7. Контентные вопросы `CONTENT-PLACES.md` §8 №1–7 и Q-02 открыты.

**Расхождения документов, не входившие в задачу (не правились):**

| № | Где | Что |
|---|---|---|
| 1 | `PRODUCT.md` 8.2 | Первый запуск описан как два экрана с «Пропустить» / «Пока не знаю»; в коде и `MVP-UX-SPEC.md` §8 — один экран, «Готово» + «Пропустить всё» |
| 2 | `PRODUCT.md` 8.3 «До поездки» | «из уже начавшихся этапов» — в коде до 3 дел по порядку этапов без учёта дат ([OQ3-6]) |
| 3 | `PRODUCT.md` 8.3 «В поездке» | «В помещении (дождь или жара)» — в коде «В помещении»; фильтр на текущей базе даёт 0 мест ([OQ3-7]) |
| 4 | `PRODUCT.md` 10.2 | Нет `js/views/task.js`; есть несуществующий `assets/icons/` |
| 5 | `PLACES-IMPLEMENTATION.md` §7, [OQ-17] | Описывает расстояние «от центра выбранного района» и открытый вопрос «~0,0 км» — исторический документ Итерации 2, не обновлялся; действующий контракт — `ITERATION-3-IMPLEMENTATION.md` §26 |
| 6 | `ITERATION-3-IMPLEMENTATION.md` §§1–25 | Сохраняют формулировки плана («от вашего района»); помечены как история, заголовок отсылает к §26 |
| 7 | UX-текст | Подпись строки списка `.info-card__summary` (расстояние · длительность · нагрузка) — 14 px (`--font-size-sm`, стиль Итераций 1–2, также в «Справке»); основной текст ≥16 px. Решением этой задачи не менялось |
| 8 | Заголовок подблока «Где именно?» | 14 px (подписи опций — 16 px) |
| 9 | Перевёрнутые даты | Сохраняются, читаются как «Без дат», форма не предупреждает |

**Долг формального закрытия по DoD `PRODUCT.md` 10.6:** публикация на GitHub Pages (п. 5), Chrome Android и Яндекс Браузер (п. 1). `PRODUCT.md` обновлён и журнал заполнен (п. 6) — выполнено этим заходом.

---

## 15. Итоговый статус Итерации 3

**Реализована, решения владельца приняты и внесены в код и документы, автоматическая validation пройдена. Формально не закрыта: ожидает результата QA на реальном iPhone.**

- Весь объём `PRODUCT.md` 11.2 (строка 3) реализован; критерий итерации выполнен в эмуляции: смена дат переключает 4 состояния Главной, сортировка по расстоянию совпадает с контрольными значениями (раздел 6.4).
- Дефект реального QA («~3,5 км» в Ялунване) исправлен моделью `stay` + `resolveOrigin()` + `spots`; «~0,0 км» заменено на «менее 0,1 км»; расчёты и сортировка не менялись.
- Модель проживания, 6 ориентиров и формат утверждены владельцем 2026-09-13; `PRODUCT.md` v1.3, `MVP-UX-SPEC.md`, `CONTENT-PLACES.md` v1.2 и `ITERATION-3-IMPLEMENTATION.md` §26.8 синхронизированы.
- Финальная validation: **278 PASS / 0 FAIL**, 0 сетевых сбросов; реальных дефектов кода нет.
- **QA на реальном iPhone после финализации — не получен** (раздел 12.2).

**Следующий шаг:** владелец проходит сценарий раздела 12.2 на реальном iPhone и передаёт результат. При отсутствии замечаний статус этого документа меняется на «закрыта (кроме долга DoD: GitHub Pages, Chrome Android, Яндекс Браузер)»; при замечаниях — точечное исправление и повтор validation. Итерация 4 до этого не начинается.
