# Iteration 8 — Draft Resolution — Result

Дата: 2026-09-16. Точечный batch по трём существующим draft-записям (`linchunling-forest-park`, `west-island`, `sanya-paddy-field-park`), продолжение `ITERATION-8-CONTENT-BATCH-1-RESULT.md`, `ITERATION-8-CONTENT-BATCH-2-RESULT.md` и `ITERATION-8-PRODUCT-BLOCKERS-REVIEW.md`. Новый общий research по Санье не проводился — для каждого места точечно проверялся именно оставшийся блокер, указанный в задании.

## Summary

| Place | Before | Result | Blocker checked | Evidence | Remaining uncertainty |
|---|---|---|---|---|---|
| Linchunling Forest Park | `draft` | **осталось `draft`** | Координата главного входа парка (не центр/не viewpoint) | Официальная страница Visit Sanya, OSM (контур парка way 675579023, отдельный POI-узел 11711684269, два viewpoint-узла, поиск entrance-узлов в районе), Baidu Baike, Ctrip (редиректы недоступны напрямую), Tencent Maps POI (нашёл адрес «凤凰路146号», но без координат), Nominatim-геокодинг адреса, автомобильный справочник парковок рядом с парком — ни один источник не даёт координату именно точки входа, отличную от контура/центра парка | Координата входа по-прежнему `unconfirmed`; `effort` понижен с `high` на `medium` (см. «Changes») |
| West Island | `draft` | **`verified`** | Координата туристического причала Сяоци (肖旗港) | Автобусная остановка OSM (node 4697256689, name:en — транслитерация «肖旗港»), проходящие через неё маршруты 16/24 совпадают с независимым описанием «24 и 16 маршруты останавливаются у причала Сяоци»; независимое географическое описание («западная оконечность Ялинь Чанлан, между Kempinski и Тяньya Хайцзяо») совпадает с найденным кластером; в этом кластере два ferry_terminal-узла в OSM в ~150–300 м друг от друга — один явно помечен как «西岛货运居民码头» (грузовой/жилой, исключён), координата взята от второго, немаркированного | Точность — уровень причальной плазы/зоны (десятки–сотни метров), не конкретного понтона; цена расходится на 5 CNY между официальным сайтом острова (95) и агрегатором (90) — использована официальная |
| Sanya Paddy Field National Park | `draft` | **осталось `draft`** | Цена входа и актуальный статус работы | Официальная страница Visit Sanya (часы 08:30–22:00, цены нет), haikou.bendibao (часы 08:30–18:00 днём, **прямое указание «夜場: 暂停营业» — вечерний сеанс временно не работает**), повторный поиск подтвердил приостановку регулярного вечернего шоу и наличие отдельного разового события (Фонарный фестиваль Хайтан, 30.01–08.03.2026, официальная страница Visit Sanya) вместо него | Новый, более серьёзный блокер, чем был: не просто цена не найдена, а под вопросом сама актуальность вечернего шоу — ключевого `why`/`bestTime` карточки на дату проверки |

## Changes

В `data/places.json`:

1. **`linchunling-forest-park`** (статус не менялся, остаётся `draft`):
   - `effort`: `"high"` → `"medium"`. Обоснование: независимые источники описывают маршрут от главного входа до смотровой башни как ~2 км с набором высоты, а `durationHours` карточки — `[2, 3]` часа; это соответствует определению `medium` в `PRODUCT.md` (§9.3: «прогулка 1–3 км или заметные подъёмы»), а не `high` («длительная ходьба, трекинг, крутые подъёмы»). Полный контурный маршрут 5,5 км/~4 ч существует, но не то, что описывает текущая карточка (`durationHours`), поэтому `effort: high` не был оправдан именно этими данными — по прямому указанию задания не менялся «ради появления high», а пересмотрен по существу в другую сторону.
   - `price.note`: уточнена формулировка — несколько независимых источников (Baidu Baike и др.) согласованно описывают парк как бесплатный, но официальная страница Visit Sanya цену не публикует вовсе; прямого официального подтверждения по-прежнему нет, `price.type` остаётся `unknown`.
   - `hours`: расширена формулировка разброса между источниками (07:00–21:00 / 07:00–21:30 у разных агрегаторов против 07:00–22:30 у Visit Sanya).
   - `tips`: заменена первая запись — теперь явно перечисляет, какие источники были точечно проверены на этот раз и почему координата входа не была найдена; убрано упоминание бесплатности из второго пункта (перенесено логикой в `price.note`).
   - `location` не менялась (`{lat:0, lng:0}` — по-прежнему заглушка, запись не показывается пользователю благодаря `status: draft`, D-18).

2. **`west-island`** (статус: `draft` → `verified`):
   - `location`: `{lat: 0, lng: 0}` → `{lat: 18.292664, lng: 109.365099}` — координата ferry_terminal-узла OSM в подтверждённом кластере причала Сяоци, отдельная от помеченного грузового/жилого причала.
   - `hours`: добавлено новое поле (ранее отсутствовало) — расписание парома по независимым источникам, с оговоркой, что официальный сайт острова точное расписание не публикует.
   - `price.note`: добавлена оговорка про расхождение 95 CNY (официальный сайт) vs 90 CNY (Baidu Baike), с явным указанием, что использован официальный сайт как более сильный источник.
   - `tips`: черновиковый пункт про неподтверждённую координату заменён на два новых — подробное обоснование координаты причала (с явным указанием, что грузовой/жилой причал исключён) и honest-оговорка про точность на уровне причальной зоны, а не конкретного понтона.
   - `sources`: добавлены два источника OSM (автобусная остановка и ferry_terminal-узел), использованные для подтверждения координаты.
   - `addressZh`, `price.type` (`paid`), `durationHours`, `effort`, `bestTime` — не менялись, проверены и остаются согласованными с источниками.

3. **`sanya-paddy-field-park`** (статус не менялся, остаётся `draft`):
   - `hours`: переписано — теперь явно указывает на расхождение между официальной страницей (08:30–22:00 без оговорок) и независимым источником (дневной сеанс 08:30–18:00, вечерний сеанс временно приостановлен).
   - `bestTime`: `"Вечер — вечернее световое шоу"` → `"Требует уточнения на месте — см. tips про приостановку вечернего сеанса"` — прежняя формулировка опиралась на функцию (вечернее шоу), актуальность которой не подтвердилась при повторной проверке.
   - `tips`: добавлены два новых пункта, подробно описывающих находку (приостановка вечернего сеанса, отдельное разовое событие вместо него, отсутствие базовой цены на вход при наличии только пакетных билетов от 188 CNY).
   - `sources`: добавлены два источника — независимый агрегатор (часы/статус вечернего сеанса) и официальная страница отдельного события (Фонарный фестиваль), чтобы не путать его с регулярным шоу из `why`.
   - `location`, `addressZh`, `price.type` (`unknown`), `durationHours`, `effort`, `category` — не менялись.

Никакие другие записи, `config.json`, `PRODUCT.md`, схема, UI, JS/CSS, `plan.json` и механика nearby/GPS не затрагивались.

## Sources

**Linchunling Forest Park:**
- Sanya Tourism Board (официальный, EN+zh) — адрес, часы, отсутствие цены.
- OpenStreetMap way 675579023 (контур парка), node 11711684269 (POI-точка «临春岭森林公园»), viewpoint-узлы 4424703289 / 5271272851, поиск entrance-тегированных узлов в радиусе парка — ни один не относится именно к этому парку/его входу.
- Baidu Baike, Ctrip (через поисковые сниппеты — прямой fetch заблокирован/редиректил), 360 карты, Tencent Maps POI (map.qq.com) — согласованно подтверждают бесплатность и адрес «凤凰路146号», без координат.
- Nominatim (openstreetmap.org geocoder) по адресу «凤凰路146号 三亚» — пустой результат.
- Автомобильный справочник парковок (icauto.com.cn) — ближайшая парковка в 318 м от парка, не является входом.
- Качество: официальный источник хороший для текстовых фактов, но не даёт координат; агрегаторы взаимно согласуются по бесплатности, но не дают точку входа — недостаточно для снятия блокера.

**West Island:**
- Sanya Tourism Board — официальный, адреса острова и причала.
- Официальный сайт острова syxidao.com — цена билета (недоступен для WebFetch напрямую из-за несовпадения сертификата, но цена уже была подтверждена в предыдущем батче и не пересматривалась).
- OpenStreetMap node 4697256689 (автобусная остановка, name:en — транслитерация «肖旗港», маршруты 6;14;16;24;25;26;32;55;57) — независимое, хорошее качество (совпадение с независимым текстовым описанием маршрутов 16/24 к причалу).
- OpenStreetMap node 5683003447 (ferry_terminal, без имени, в кластере с явно помеченным грузовым/жилым причалом node 13525380802) — независимое, качество среднее (нет собственного имени, но локация подтверждена смежными объектами и элиминацией).
- Поисковые агрегаторы (cncn.com, sanya.bendibao.com — через сниппеты) — независимое географическое описание «западная оконечность Ялинь Чанлан, между Kempinski и Тяньya Хайцзяо», совпадающее с найденным кластером; также источник цифры 90 CNY (используется только для фиксации расхождения, не как основной источник цены).

**Sanya Paddy Field National Park:**
- Sanya Tourism Board — официальный, часы 08:30–22:00 без оговорок, цены нет.
- 海口本地宝 (haikou.bendibao.com/tour/70277.shtm) — независимый агрегатор, свежая проверка: дневной сеанс 08:30–18:00, прямое указание на приостановку вечернего сеанса («夜场：暂停营业»). Качество: независимое, специализированное на актуальной операционной информации (часы/статус), совпадает по формулировке с отдельным повторным поисковым запросом.
- Sanya Tourism Board — страница события «Фонарный фестиваль Хайтан 2026» (event/24390) — официальный источник, подтверждает, что вечерняя программа в январе–марте 2026 существует как отдельное сезонное событие, а не как регулярное шоу, упомянутое в карточке.
- Билетный мини-программный сайт (1407149mtty.sjdzp.cn) — только пакетные билеты (188 CNY, включают аттракцион динозавров и воздушный шар), не является источником базовой цены на вход — не использован как источник цены в карточке, только зафиксирован факт отсутствия базовой цены.

## Validation

1. **JSON валиден** — `python3 -m json.tool data/places.json` без ошибок.
2. **Уникальность id** — 22 записи, 22 уникальных id.
3. **Draft не показываются пользователю** — headless-проверка (`#/places`): список показывает ровно 18 verified-мест (включая West Island), «Линьчуньлин» и «Рисовые поля» в тексте страницы отсутствуют.
4. **Координаты `{lat:0, lng:0}`** — проверено для всех трёх мест: у `west-island` (переведён в verified) координата реальная `(18.292664, 109.365099)`; `linchunling-forest-park` и `sanya-paddy-field-park` остались `draft`, поэтому нулевая/заглушечная координата у `linchunling-forest-park` не является проблемой (D-18 скрывает draft).
5. **Экран списка и карточки** — headless Chrome (325×667, `--headless=new`), самодельный CDP-клиент на Python stdlib, `ThreadingTCPServer` на порту 8793: `#/places` рендерится и содержит 18 карточек мест; карточка `#/place/west-island` рендерится с непустым контентом.
6. **«Показать таксисту» для West Island** — текст карточки содержит `肖旗港` (китайский адрес причала) — доступен для практического сценария.
7. **Прямой переход на draft-карточку** (`#/place/linchunling-forest-park`) не роняет приложение.
8. **Browser Back** — переход `/places → /place/west-island → history.back()` меняет `location.hash` (уходит с карточки места), история не ломается.
9. **localStorage** — доступен и не бросает исключений при взаимодействии с карточкой West Island.
10. **Горизонтальный overflow** — на ширине 375px `scrollWidth − clientWidth = 0` и на `#/places`, и на карточке `west-island`.
11. **`git diff`** — изменён только `data/places.json` (в дополнение к уже накопленным изменениям Batch 1–2 в этом же файле, не тронутым в этом batch); `config.json`, `PRODUCT.md`, `plan.json`, JS/CSS не менялись.
12. **`git status`** — см. вывод ниже; никакие новые файлы, кроме этого отчёта, не создавались (фото не добавлялись, story не добавлялись).

Все проверки — **PASSED**.

## Final counts

- **Verified: 18** (было 17, +1 — West Island).
- **Draft: 4** (было 5, −1 — Bohou Village, Linchunling Forest Park, Sanya Paddy Field National Park, Atlantis Sanya Aquarium остаются `draft`).
- **Всего записей: 22** (без изменений).

## Remaining draft blockers

- **Linchunling Forest Park:** координата главного входа парка не подтверждена ни одним проверенным источником (официальным, картографическим или агрегаторным) — все найденные точки относятся к контуру парка, POI-центру или смотровым площадкам, что задание прямо запрещает использовать как замену входу. Официального подтверждения бесплатного входа также по-прежнему нет (хотя косвенно это устойчиво повторяется в независимых источниках). Требуется либо более сильный картографический источник (например, официальный сайт с картой-схемой парка, отсутствующий на данный момент), либо прямая проверка на месте.
- **Sanya Paddy Field National Park:** появился новый, более существенный блокер — заявленная в карточке ключевая причина посещения (регулярное вечернее световое шоу) на дату проверки, по независимому источнику, не работает в обычном режиме («夜场：暂停营业»); вместо неё в начале 2026 года шло отдельное сезонное мероприятие. Цена входа по-прежнему нигде не опубликована как единая цифра (только пакетные билеты). До прояснения статуса вечернего сеанса и общей актуальности заявленного формата перевод в `verified` не оправдан.
- **Atlantis Sanya Aquarium** (не входил в задание этого batch, статус не менялся) — остаётся `draft` по итогам Batch 2, блокер там — конфликтующие часы работы и недоступный официальный сайт объекта.

## Next batch (только рекомендации)

- Для Linchunling — если владелец готов принять менее строгий стандарт для координаты (например, координату ближайшей подтверждённой точки на Fenghuang Road рядом с известным домовым номером 146, с явной оговоркой о приблизительности), это могло бы снять блокер другим способом, чем чистая находка точной точки входа; это решение по стандарту качества, а не находка новых фактов.
- Для Paddy Field Park — стоит через несколько недель/месяцев (после окончания сезонного Фонарного фестиваля 08.03.2026) перепроверить, возобновился ли обычный вечерний сеанс, прежде чем снова пытаться перевести запись в `verified`; если сеанс не возобновится, карточку в её нынешнем виде («вечернее шоу» как главная причина посещения) стоит переформулировать под дневной сценарий (рисовые поля + динозавры без акцента на шоу), это уже решение по контенту, а не по факту.
- Фотографии для новых verified-мест (в частности, для West Island) стоит поискать отдельно, не совмещая с контентным батчем — бюджет (10 файлов / 600 КБ) по-прежнему не исчерпан.

---

## Git (конец задания)

Commit и push не выполнялись — изменения оставлены в рабочем дереве, как в предыдущих content-батчах этой итерации.
